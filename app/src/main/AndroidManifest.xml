<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.coletaneaicm.coletanea.coletaneaicm"
    android:versionCode="1"
    android:versionName="1.0">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ColecoesActivity"
            android:label="@string/title_activity_colecoes"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CategoriasActivity"
            android:label="@string/title_activity_categorias"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MusicaActivity"
            android:label="@string/title_activity_musica"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SingleMusicaActivity"
            android:label="@string/title_activity_single_musica"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RegistroActivity"
            android:label="@string/title_activity_registro"

            android:theme="@style/Theme.Design.NoActionBar"></activity>
    </application>

</manifest>